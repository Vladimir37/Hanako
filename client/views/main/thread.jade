extends ../basis/layout

block content
    h1 /#{boards_data[0]}/ — #{boards_data[1]}
    mixin posting_form()
    section.thread
        mixin op_post(op_post)
        - each post, i in posts
            mixin post(post)
    mixin posting_form()